{
  "name": "user-contract-interaction",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ airstack-xyz/web3-feed",
    "create-local": "graph create --node http://localhost:8020/ airstack-xyz/web3-feed",
    "remove-local": "graph remove --node http://localhost:8020/ airstack-xyz/web3-feed",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 airstack-xyz/web3-feed"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.4",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
