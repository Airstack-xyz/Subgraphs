{
  "name": "opensea-wyvern-exchange",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ opensea-wyvern-exchange",
    "create-local": "graph create --node http://localhost:8020/ opensea-wyvern-exchange",
    "remove-local": "graph remove --node http://localhost:8020/ opensea-wyvern-exchange",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 opensea-wyvern-exchange",
    "test": "graph test"
  },
  "dependencies": {
    "@airstack/subgraph-generator": "^1.0.2-alpha.2",
    "@graphprotocol/graph-cli": "0.35.0",
    "@graphprotocol/graph-ts": "0.28.1",
    "@protofire/subgraph-toolkit": "^0.1.2"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
